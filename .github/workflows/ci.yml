name: CI

# Runs on every push and pull request. The publish workflow waits for this to
# pass before it builds an image, so nothing broken reaches Docker Hub.

on:
  push:
    branches: [main]
  pull_request:
  workflow_call:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  php:
    name: PHP tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: pdo_sqlite, sqlite3, gd, exif, intl, zip, mbstring
          coverage: none
          tools: composer:v2

      - name: Cache Composer packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/composer/files
          key: composer-${{ hashFiles('composer.lock') }}
          restore-keys: composer-

      - run: composer install --prefer-dist --no-interaction --no-progress

      # .env is git-ignored, as it must be, so the runner makes its own from
      # the template. The key is generated per run rather than committed
      # anywhere: a fixed test key in phpunit.xml would be a tracked secret,
      # which is exactly the habit this repository has stopped.
      - name: Prepare the environment
        run: |
          cp .env.example .env
          php artisan key:generate

      - name: Check code style
        run: ./vendor/bin/pint --test

      - name: Run the test suite
        run: php artisan test

  frontend:
    name: Front-end checks
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - run: npm ci --no-audit --no-fund

      - name: Type check
        run: npm run types

      - name: Lint
        run: npm run lint

      - name: Check formatting
        run: npm run format:check

      - name: Unit tests
        run: npm run test

      - name: Production build
        run: npm run build

  e2e:
    name: End-to-end tests
    runs-on: ubuntu-latest
    needs: [php, frontend]

    steps:
      - uses: actions/checkout@v4

      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: pdo_sqlite, sqlite3, gd, exif, intl, zip, mbstring
          coverage: none
          tools: composer:v2

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - run: composer install --prefer-dist --no-interaction --no-progress
      - run: npm ci --no-audit --no-fund

      # global-setup.ts builds .env.e2e from its own template, but the base
      # environment and the public/storage symlink still have to exist for the
      # served application to find its uploaded photos.
      - name: Prepare the environment
        run: |
          cp .env.example .env
          php artisan key:generate
          php artisan storage:link

      # The suite serves the real application, so it needs real assets.
      - run: npm run build

      - name: Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ hashFiles('package-lock.json') }}

      # WebKit is the one that matters: it is what an iPhone runs.
      - name: Install browsers
        run: npx playwright install --with-deps chromium webkit

      - run: npm run test:e2e

      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
